#!/usr/bin/env python3

# Asks for user input to define hostname
hostname = input("What value should we set for hostname?")

# If the user input matches the hostname, the text below will be output
if hostname == "MTG":
    print("The hostname was found to be mtg")

